---
id: intro
title: Overview
sidebar_label: Introduction
---

import styles from './Styles.module.css';
import { FaShieldAlt, FaCogs, FaCloud, FaCodeBranch, FaDatabase, FaDocker, FaLock, FaTools, FaUserShield } from 'react-icons/fa';

export const cards = [
  { title: 'Security', icon: <FaShieldAlt />, text: 'End-to-end security from code to cloud.' },
  { title: 'CI/CD', icon: <FaCogs />, text: 'Automate build and deploy pipelines.' },
  { title: 'Cloud Infra', icon: <FaCloud />, text: 'Manage scalable cloud infrastructure.' },
  { title: 'Source Control', icon: <FaCodeBranch />, text: 'Integrated Git workflows.' },
  { title: 'Database Ops', icon: <FaDatabase />, text: 'Provision and manage DBs.' },
  { title: 'Docker Build', icon: <FaDocker />, text: 'Build, tag, and push Docker images.' },
  { title: 'Secrets Mgmt', icon: <FaLock />, text: 'Handle secrets securely in pipelines.' },
  { title: 'Tooling', icon: <FaTools />, text: 'Useful developer tools & scripts.' },
  { title: 'Access Control', icon: <FaUserShield />, text: 'Role-based access & audit logs.' },
];

<div className={styles.workflowHeader}>
  <h1>Choose a workflow</h1>
  <p>
    Build, test, and deploy your code. Make code reviews, branch management, and issue triaging work the way you want. Select a workflow to get started.
  </p>
  <p>
    <a href="#" className={styles.skipLink}>Skip this and set up a workflow yourself</a>
  </p>

  <input
    type="text"
    placeholder="Search workflows"
    className={styles.searchBar}
  />

  <h2 className={styles.suggestionHeading}>Suggested for this repository</h2>
</div>

<main className={styles.container}>
  <div className={styles.grid}>
    {cards.map((card, idx) => (
      <div className={styles.card} key={idx}>
        <div className={styles.icon}>{card.icon}</div>
        <h3 className={styles.title}>{card.title}</h3>
        <p className={styles.text}>{card.text}</p>
      </div>
    ))}
  </div>
</main>
