---
id: continuous-integration
title:  Automate Continuous Integration
sidebar_label: Continuous Integration
---

import React, { useState } from "react";
import styles from "./Styles.module.css";
import { FiSearch } from 'react-icons/fi';

import Cardsintegration from "./Cardsintegration";

export default function CICDWorkflowHub() {
  const [searchTerm, setSearchTerm] = useState("");

  return (
    <>
      <div className={styles.workflowHeader}>
        <p>
         Speed up development with automated testing, building, and integration of code changes through CI tools like GitHub Actions, Jenkins, and GitLab CI.
        </p>

        <div className={styles.searchContainer}>
             <FiSearch className={styles.searchIcon} />
                <input
                     type="text"
                     placeholder="Search workflows"
                     className={styles.searchBar}
                     value={searchTerm}
                     onChange={(e) => setSearchTerm(e.target.value)}
                 />
        </div>


      </div>

      <Cardsintegration searchTerm={searchTerm} />
    </>
  );
}


