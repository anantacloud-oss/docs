---
id: iac
title: Manage Infrastructure as Code
sidebar_label: IAC
---


import React, { useState } from "react";
import styles from "./Styles.module.css";
import { FiSearch } from 'react-icons/fi';

import Cardsiac from "./Cardsiac";

export default function CICDWorkflowHub() {
  const [searchTerm, setSearchTerm] = useState("");

  return (
    <>
      <div className={styles.workflowHeader}>
        <p>
         Automate and version your cloud infrastructure using tools like Terraform, Pulumi, and Terragrunt. Improve consistency, scalability, and reproducibility across environments.
        </p>

        <div className={styles.searchContainer}>
             <FiSearch className={styles.searchIcon} />
                <input
                     type="text"
                     placeholder="Search workflows"
                     className={styles.searchBar}
                     value={searchTerm}
                     onChange={(e) => setSearchTerm(e.target.value)}
                 />
        </div>


      </div>

      <Cardsiac searchTerm={searchTerm} />
    </>
  );
}


