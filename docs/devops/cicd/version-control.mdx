---
id: version-control
title: Manage Code with Version Control
sidebar_label: Version Control
---


import React, { useState } from "react";
import styles from "./Styles.module.css";
import { FiSearch } from 'react-icons/fi';

import Cardsversion from "./Cardsversion";

export default function CICDWorkflowHub() {
  const [searchTerm, setSearchTerm] = useState("");

  return (
    <>
      <div className={styles.workflowHeader}>
        <p>
          Track changes, collaborate efficiently, and maintain code history using distributed version control systems like Git for reliable and organized development.
        </p>

        <div className={styles.searchContainer}>
             <FiSearch className={styles.searchIcon} />
                <input
                     type="text"
                     placeholder="Search workflows"
                     className={styles.searchBar}
                     value={searchTerm}
                     onChange={(e) => setSearchTerm(e.target.value)}
                 />
        </div>


      </div>

      <Cardsversion searchTerm={searchTerm} />
    </>
  );
}


