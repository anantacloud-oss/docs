---
id: Amazon-Cluster
title: Amazon ECS Cluster
sidebar_label: Amazon ECS Cluster
---

import ECSClusterModal from './ECSClusterModal';

## Amazon ECS Cluster

<ECSClusterModal />

## Amazon ECS Cluster

---

### Overview

This service contains Terraform code to deploy a production-grade ECS cluster on AWS using Elastic Container Service (ECS).

This service launches an ECS cluster on top of an Auto Scaling Group that you manage. If you wish to launch an ECS cluster on top of Fargate that is completely managed by AWS, refer to the `ecs-fargate-cluster` module. Refer to the section **EC2 vs Fargate Launch Types** for more information on the differences between the two flavors.

#### ECS architecture
*ECS architecture*

---

### Features

This Terraform Module launches an EC2 Container Service Cluster that you can use to run Docker containers. The cluster consists of a configurable number of instances in an Auto Scaling Group (ASG). Each instance:

- Runs the ECS Container Agent so it can communicate with the ECS scheduler.
- Authenticates with a Docker repo so it can download private images (using KMS encrypted details).
- Runs the CloudWatch Logs Agent to send logs in syslog to CloudWatch.
- Emits custom metrics (memory, disk, etc.) via the `cloudwatch-agent`.
- Uses the `syslog` module for log rotation and disk usage protection.
- Uses `ssh-grunt` for IAM-based SSH access to ECS nodes.
- Uses `auto-update` module for security updates.

---

### Learn

:::info Note
This repo is a part of the Ananta Cloud **Service Catalog**, a collection of reusable, battle-tested, production-ready infrastructure code.  
If you’ve never used the Service Catalog before, read **How to use the Ananta Cloud Service Catalog!**
:::

This is implemented using Terraform modules from the `terraform-aws-ecs` repo.  
If you are a subscriber and don’t have access, email [support@anantacloud.io](mailto:support@anantacloud.io).

---

### Core Concepts

To understand what ECS is and the different cluster types, refer to the documentation in the `terraform-aws-ecs` repo.

To use ECS:

- Deploy EC2 Instances into a **cluster**.
- ECS scheduler runs Docker containers across instances.
- Each instance must have ECS Agent installed.

Also refer to:

- `ecs-cluster` docs for running Docker containers, IAM, security groups, etc.
- `cloudwatch-agent` docs for logs and metrics.

---

### Repo Organization

- **modules**: main implementation code (submodules).
- **examples**: working usage examples.
- **test**: automated tests for modules and examples.

---

### Deploy

#### 🚀 Non-production deployment (learning/testing)

Refer to:

- `examples/for-learning-and-testing`: optimized for experimenting and learning.

#### 🏢 Production deployment

Refer to:

- `examples/for-production`: sample production-ready deployments based on Ananta Cloud Reference Architecture.
